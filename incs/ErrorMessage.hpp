#pragma once

#include <string> 

const std::string WrongPassword = "Error : The password is incorrect\n";
const std::string NoPassword = "Error : You didn't enter the password to access to the server\n";